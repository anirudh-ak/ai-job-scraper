[project]
name = "ai-job-scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "requests",
  "pandas",
  "beautifulsoup4"
]
